diff --git a/onnxruntime/__init__.py b/onnxruntime/__init__.py
index b17e980..070a1d5 100644
--- a/onnxruntime/__init__.py
+++ b/onnxruntime/__init__.py
@@ -79,7 +79,6 @@ if import_capi_exception:
 
 from onnxruntime.capi.onnxruntime_inference_collection import (
     AdapterFormat,  # noqa: F401
-    InferenceSession,  # noqa: F401
     IOBinding,  # noqa: F401
     ModelCompiler,  # noqa: F401
     OrtDevice,  # noqa: F401
@@ -95,6 +94,25 @@ except ImportError:
     pass
 
 
+import js
+#  Copied from:
+# https://github.com/pyodide/pyodide-recipes/issues/272#issuecomment-3451188602
+js.importScripts('https://cdn.jsdelivr.net/npm/onnxruntime-web/dist/ort.min.js')
+js.ort.env.wasm.wasmPaths = "https://cdn.jsdelivr.net/npm/onnxruntime-web/dist/"
+
+# TODO implement as much of the API described here:
+#      https://onnxruntime.ai/docs/api/python/api_summary.html
+class InferenceSession:
+
+    def __init__(self, *args):
+        self.session_js = await ort.InferenceSession.create(*args)
+
+    def run(self, *args):
+        results = await self.session_js.run(to_js(args))
+        return results.to_py()
+    
+
+
 package_name, version, cuda_version = onnxruntime_validation.get_package_name_and_version_info()
 
 if version:
